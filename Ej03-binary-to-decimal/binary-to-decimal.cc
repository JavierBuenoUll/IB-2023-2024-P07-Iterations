/**
  * Universidad de La Laguna
  * Escuela Superior de Ingeniería y Tecnología
  * Grado en Ingeniería Informática
  * Informática Básica 2023-2024
  *
  * @file binary-to-decimal.cc
  * @author Javier Bueno Calzadilla alu0101627922@ull.edu.es
  * @date 26/10/2023
  * @brief This program converts a binary number to decimal.
  * @bug There are no known bugs
  */

#include <iostream>
#include <cmath>

int main () {

  int binary_number;
  std::cout << "Introduce el número que quieras pasar a decimal" << std::endl;
  std::cin << binary_number;
  int resto = 0;
  
  if (binary_number < 0) {
    std::cout << "Wrong Imput" << std::endl;
  return 1;
}
  int digitos_numero = binary_number;
    while (digitos_numero != 0) {
      digitos_numero / 10
      digitos_numero++
  }
  for (int i = binary.length() - 1; i >= 0; i--) {
    std::cout << binary[i];
  }    

}
  }
  int decimal_number; 
    for (int i = 0; i < digitos_numero; i++ && digitos_numero--) {
    int digito =  * (std::pow(2,i);
  
